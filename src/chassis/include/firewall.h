#pragma once

// #include <vector>
#include <utils/point.h>

namespace chassis
{
    class firewall
    {
        public:
        
        private:
            double tolerance;
            std::unordered_map<QString, point> pointMap;
    }
}